import styled from "styled-components";

const EditIdContainer = styled.div`
  width: 100vw;
  height: calc(100vh - 4rem);

  padding: 2em 10vw;

  background: var(--background);
`;

export { EditIdContainer }